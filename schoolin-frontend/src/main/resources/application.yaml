server:
  port: 5250
  
spring:
  jackson:
    date-format: "YYYY-MM-dd"
    serialization:
      write-dates-as-timestamps: false
      indent-output: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
      
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:6603/schoolin
    username: root
    password: changeme
  data:    mongodb:      authentication-database: schoolin
      username: schoolin
      password: welcome
      database: schoolin
      port: 27017
      host: localhost

# jwt secret
shuaicj.security.jwt.secret: otherpeopledontknowit